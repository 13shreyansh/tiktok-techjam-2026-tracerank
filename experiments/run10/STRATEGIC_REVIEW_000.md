# Run 10 strategic review 000

The fresh-context priority is censored watch time, not another listwise or
sequence rerun. The starter explicitly identifies CWM as unexplored headroom;
Run 5 already rejected sampled listwise and BPR, Run 7 rejected explicit
multi-behavior histories, and Run 2's watch-ratio auxiliary did not model
right-censoring. Run 9 also confirmed that generic capacity is not the current
bottleneck.

The experiment preserves the judged binary target and uses CWM only as an
auxiliary representation signal. This avoids replacing the official
`long_view` definition with the paper's reconstructed `long_view2` label. One
fixed upstream parameterization and one conservative auxiliary weight are
allowed before a stop/promotion decision.
