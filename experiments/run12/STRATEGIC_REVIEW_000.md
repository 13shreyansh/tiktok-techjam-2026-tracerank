# Run 12 strategic review 000

The official category taxonomy is genuinely new information: 388 semantic
paths versus 111 starter tag combinations. It is selected only after sequence,
behavior, ranking-loss, temporal, generic-capacity, and watch-time families
failed. Captions are not acquired because their full source is much larger and
would introduce a separate text-model hypothesis.

One fixed hierarchy representation is allowed. A result that helps only the
early window or only cold users will not be promoted. The Pure subset is exact,
ignored, reproducible by byte range, and licensed/provenanced separately.
