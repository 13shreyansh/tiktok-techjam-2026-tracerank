# Run 83 decision journal

## 2026-08-31 16:14 SGT — paired chronology audit selected

- Run82's all-causal official ensemble is `+0.0001203396` above the protected
  mixed ensemble and passes every fixed official slice, but misses its
  precommitted score-materiality gate. Official validation cannot distinguish
  a small transferable improvement from seed noise.
- Isolate the unresolved mechanism directly: matched source-order versus
  causal histories, exact architecture and seeds, on three chronological
  validation/forward windows. Source-order is a diagnostic comparator only.
- Require two-of-three window support, paired seed evidence, both metric
  components, and every established slice. Do not reopen official validation,
  search weights/subsets, or change either frozen artifact.
- Third-person goal check: this resolves which existing required-Pure artifact
  is more likely to transfer. It advances winning probability more directly
  than another optional-benchmark or minor-capacity run.

## 2026-08-31 16:23 SGT — early window supports causal history

- All eight attempts succeeded: three matched source/causal seeds and two fixed
  equal user-rank consensuses on `shadow_early`; no official/public/hidden
  labels were evaluated.
- The causal consensus improves validation primary by `+0.0002935404` and
  forward primary by `+0.0002778589`. Both metric components improve on both
  periods, all three paired validation deltas are positive, and all fixed slice
  changes stay above the `-0.001` floor.
- This is one supporting window, not promotion evidence by itself. Continue
  unchanged to `shadow_middle`; keep both frozen official candidates intact.
- Mandatory independent review: `STRATEGIC_REVIEW_008.md`.

## 2026-08-31 16:29 SGT — middle window fails forward gate

- The causal consensus improves validation primary by `+0.0002156916`, but
  regresses forward primary by `-0.0002142136`; the window therefore fails the
  frozen requirement to improve both.
- Two of three paired validation seeds improve and no component or slice hits
  a catastrophic floor. This is a bounded failure, not a reason to stop before
  the decisive third window.
- State after two windows: early supports causal; middle does not. Continue
  unchanged to `shadow_late`; do not promote or modify either official artifact.
- Mandatory independent review: `STRATEGIC_REVIEW_016.md`.

## 2026-08-31 16:35 SGT — late pass selects frozen all-causal artifact

- The late causal consensus improves validation primary by `+0.0000093049`
  and forward primary by `+0.0001408403`; all three paired validation seeds are
  positive, and all component and slice floors pass.
- Cross-window result is two supports (`early`, `late`) and one bounded failure
  (`middle`). The frozen protocol therefore selects the already frozen Run82
  six-causal artifact at official-validation primary `0.6055212247`.
- Keep the exact Run2 mixed artifact at `0.6054008850` as immutable fallback.
  Hidden-test score remains unknown; nothing was submitted or uploaded.
- Final mandatory review: `STRATEGIC_REVIEW_024.md`.
