# Strategic audit — 2026-08-31 07:18 SGT

Run53 establishes a capacity ceiling at rank 32, while Run54 shows that a
previously positive rank-8 topic feature does not transfer to that stronger
architecture. More parameter or feature micro-tuning would have low expected
robustness. The remaining cheap independent question is whether the protected
Run49 mixed-capacity consensus retains complementary errors to Run52.

Run49 was independently positive on all three validation windows, all forward
periods, and every fixed official slice. Run52 is substantially stronger but
uses only rank-32 repeat-affinity members. A fixed 50/50 meta-consensus of the
two protected parent predictions can reduce architecture-specific variance
without training, altering either model, or selecting members after scoring.

Run55 therefore re-ranks each parent within user and gives exactly one equal
vote to Run49 and Run52. It does not test raw logits, parent duplication,
weights, subsets, routes, or calibration. Chronological shadows use the exact
available Run49 consensus and matching Run52 rank-32 seed-2027 archive;
official evaluation uses the two protected consensus archives. A strict early
gate makes this a seconds-long falsifiable test rather than validation mining.

The main risk is dilution: Run49 is weaker overall and may pull Run52 backward.
Promotion is allowed only with material aggregate gain and slice safety. The
fixed sample remains a local selection aid, not a hidden-test estimate.
