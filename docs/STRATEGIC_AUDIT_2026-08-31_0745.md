# Strategic audit — 2026-08-31 07:45 SGT

Run57 did not merely show weak sequence signal: forcing eleven recent fields
into all FM pair interactions reduced every aggregate and slice. The protected
24-field representation remains strong, and the harmful mechanism is plausibly
the dense cross-product of noisy raw recent-tag identities with every user,
video, author, and history field.

Run58 tests one architecture-level correction. The original 24 fields retain
the exact rank-32 FM interaction path. All eleven verified sequence fields stay
in the input but are excluded from latent interactions and may contribute only
through sparse linear weights. This keeps candidate-aware count/match/recency
evidence without allowing raw remembered tags to perturb every interaction.

The implementation was committed before scoring, source SHA-256 is
`ce713c76a33155d36715f20eca1a23ba3aa76f0519e2c2adb23dbc8a5d7e0724`,
and 40 targeted tests pass. No field subset, tail weight, regularizer,
initialization, rank, or optimizer search is allowed. The risk is that even
additive sequence fields are redundant or biased; chronological transfer and
all slice gates remain mandatory. Run52 is preserved.
