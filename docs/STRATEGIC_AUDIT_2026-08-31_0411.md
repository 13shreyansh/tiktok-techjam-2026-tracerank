# Fresh-context strategic audit — 2026-08-31 04:11 SGT

## Evidence that survives Run 50

Run49's equal-rank union of three rank-8 seeds and one rank-16 seed remains the
protected candidate at `0.6509565751074728`. It improved validation, forward
time, and every fixed slice in all three chronological windows. Run50 then
showed that the fixed three-rank-16-seed consensus also improved its rank-16
seed-2027 parent in every aggregate and slice on all three shadows. Its official
stage stopped because seed 2029 individually missed the frozen stability and
slice floors; no pure rank-16 official consensus was scored.

The failure does not invalidate rank-16 diversity, but it does show that a
pure high-capacity ensemble can retain cutoff-specific seed variance. The
independent, high-value question is whether the full fixed union of the two
confirmed diversity sources—three rank-8 seeds and three rank-16 seeds—can
average that variance while preserving the architecture gain. This avoids
post-hoc selection: all six members are included, including the failed official
seed 2029, and every member receives exactly one equal within-user rank vote.

## Run 51 decision

Run 51 will score only the fixed six-member union. It will not train new
models, choose subsets, search family weights, duplicate members, calibrate
scores, or alter rank normalization. Early must beat Run49 by `+0.00025` on
validation and forward before later windows unlock. At least two of three
windows must clear that gate, with aggregate and slice safety, before exactly
one official consensus may be scored. Official promotion requires
`+0.0003` primary over Run49 and no fixed slice below `-0.0005`.

This is a cheap, falsifiable ensemble-structure test using already preserved
artifacts. The deterministic 1/32 development sample remains only a local
selection aid; no hidden/public-test labels or external action are authorized.
